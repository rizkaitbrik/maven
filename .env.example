# Maven Configuration

# Path to the retriever configuration file (YAML or JSON)
# If not set, defaults to: config/retriever_config.yaml
RETRIEVER_CONFIG_PATH=config/retriever_config.yaml

# Comma-separated list of allowed directories/patterns for search
# These will be merged with the allowed_list from the config file
# Example: /Users/yourname/projects,/Users/yourname/Documents
RETRIEVER_ALLOWED_LIST=

# Additional environment variables can be added here as needed
